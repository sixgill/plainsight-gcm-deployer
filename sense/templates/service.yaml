apiVersion: v1
kind: Service
metadata:
  name: {{ include "sense.fullname" . }}
  labels:
    {{- include "sense.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sense.selectorLabels" . | nindent 4 }}
