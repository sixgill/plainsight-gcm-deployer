apiVersion: v1
kind: Service
metadata:
  name: {{ include "Plainsight.fullname" . }}
  labels:
    {{- include "Plainsight.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Plainsight.selectorLabels" . | nindent 4 }}
