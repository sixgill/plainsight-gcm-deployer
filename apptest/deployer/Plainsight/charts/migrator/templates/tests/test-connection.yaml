apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "migrator.fullname" . }}-test-connection"
  labels:
    {{- include "migrator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "migrator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
